# Copy to /etc/containers/secrets/traefik.env on bumblebee and fill in values.
# This file is a template only — never commit the real .env file.
# Get your token from https://dash.cloudflare.com/profile/api-tokens
CF_DNS_API_TOKEN=
