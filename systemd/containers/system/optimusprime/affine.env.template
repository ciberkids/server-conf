# AFFiNE secrets — copy to /etc/containers/secrets/affine.env and fill in values

# PostgreSQL password (used by both affine-postgres and affine server)
POSTGRES_PASSWORD=<strong-random-password>

# Full database connection URL (substitute password from above)
DATABASE_URL=postgresql://affine:<strong-random-password>@affine-postgres:5432/affine
