# Copy to /etc/containers/secrets/influxdb.env on optimus-prime and fill in values.
# This file is a template only — never commit the real .env file.
DOCKER_INFLUXDB_INIT_PASSWORD=
DOCKER_INFLUXDB_INIT_ADMIN_TOKEN=
