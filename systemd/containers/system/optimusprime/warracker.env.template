# Copy to /etc/containers/secrets/warracker.env on optimus-prime and fill in values.
# This file is a template only — never commit the real .env file.
# Shared by warracker.container and warracker-db.container
POSTGRES_PASSWORD=
DB_PASSWORD=
SECRET_KEY=
