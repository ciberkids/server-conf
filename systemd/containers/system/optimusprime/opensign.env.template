# Copy to /etc/containers/secrets/opensign.env on optimus-prime and fill in values.
# This file is a template only — never commit the real .env file.
# Generate a P12 cert: openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 3650 -nodes -subj "/CN=OpenSign"
#                      openssl pkcs12 -export -out cert.p12 -inkey key.pem -in cert.pem -passout pass:yourpassphrase
#                      base64 -w 0 cert.p12
PFX_BASE64=
PASS_PHRASE=
